name: 5-Minute Shift Sync

on:
  schedule:
    - cron: '*/5 * * * *'  # 5分に1回実行
  workflow_dispatch:       # 手動実行ボタン

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Call Sync API
        run: |
          curl -X GET "https://karinto-castmanager.vercel.app/api/sync"
